# Sales Order API Integration - Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           Frontend (React)                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Sales Order Components Layer                     │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  • CreateSalesOrderModal.tsx                                 │  │
│  │    └─ Form with new fields (Sales Order Type, Lead Number,  │  │
│  │       Work Order Tenure, Project Name, Issue Date, etc.)     │  │
│  │                                                               │  │
│  │  • SalesOrderList.tsx                                        │  │
│  │    └─ Displays all sales orders from API                     │  │
│  │                                                               │  │
│  │  • SalesOrderDetails.tsx                                     │  │
│  │    └─ Shows full details, contacts, comments                 │  │
│  │                                                               │  │
│  │  • SalesOrderApproval.tsx                                    │  │
│  │    └─ Approve/reject workflow                                │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              ↕                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              API Utility Layer (NEW)                          │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  src/utils/salesOrderApi.ts                                  │  │
│  │  ================================                             │  │
│  │                                                               │  │
│  │  ✅ getAllSalesOrders()                                      │  │
│  │  ✅ getSalesOrderById(id)                                    │  │
│  │  ✅ createSalesOrder(data)                                   │  │
│  │  ✅ updateSalesOrder(id, data)                               │  │
│  │  ✅ deleteSalesOrder(id)                                     │  │
│  │  ✅ approveSalesOrder(id, reason)                            │  │
│  │  ✅ rejectSalesOrder(id, reason)                             │  │
│  │  ✅ getMaterialTypes()                                       │  │
│  │  ✅ getPaymentTermTypes()                                    │  │
│  │  ✅ getSalesOrderContactDetails(id)                          │  │
│  │  ✅ getSalesOrderComments(id)                                │  │
│  │  ✅ addSalesOrderComment(id, comment)                        │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              ↕                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Environment Configuration                         │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  .env                                                         │  │
│  │  ====                                                         │  │
│  │  VITE_API_BASE_URL=http://localhost:7320/api                │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
└───────────────────────────────┬───────────────────────────────────┘
                                │
                         HTTP Requests
                         (axios)
                                │
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        Backend API Server                            │
│                     (http://localhost:7320/api)                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                   API Endpoints                               │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  GET    /sales-order                    (All orders)         │  │
│  │  GET    /sales-order/:id                (Single order)       │  │
│  │  POST   /sales-order                    (Create)             │  │
│  │  PUT    /sales-order/:id                (Update)             │  │
│  │  DELETE /sales-order/:id                (Delete)             │  │
│  │  PATCH  /sales-order/:id/approve        (Approve)            │  │
│  │  PATCH  /sales-order/:id/reject         (Reject)             │  │
│  │                                                               │  │
│  │  GET    /sales-order-material-type-details  (Material types) │  │
│  │  GET    /sales-order-payment-terms      (Payment terms)      │  │
│  │  GET    /sales-order-contact-details/:id    (Contacts)       │  │
│  │  GET    /sales-order-comments/:id       (Comments)           │  │
│  │  POST   /sales-order-comments           (Add comment)        │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Creating a Sales Order

```
User fills form
     │
     ↓
┌─────────────────────────────────────────────┐
│   CreateSalesOrderModal.tsx                 │
│                                             │
│   1. User selects Sales Order Type         │
│   2. Enters Lead Number                    │
│   3. Selects Material Type (from API)      │
│   4. Selects Payment Terms (from API)      │
│   5. Fills all other fields                │
│   6. Clicks "Create Sales Order"           │
└─────────────────────────────────────────────┘
     │
     ↓
┌─────────────────────────────────────────────┐
│   handleSubmit()                            │
│                                             │
│   • Maps form data to API structure        │
│   • Converts frontend field names          │
│     to backend field names                 │
│   • Validates required fields              │
└─────────────────────────────────────────────┘
     │
     ↓
┌─────────────────────────────────────────────┐
│   createSalesOrder()                        │
│   (salesOrderApi.ts)                        │
│                                             │
│   • Calls POST /sales-order                │
│   • Sends JSON payload                     │
│   • Returns response                       │
└─────────────────────────────────────────────┘
     │
     ↓
Backend API
     │
     ↓
Database
     │
     ↓
Success Response
     │
     ↓
┌─────────────────────────────────────────────┐
│   UI Updates                                │
│                                             │
│   • Shows success notification             │
│   • Closes modal                           │
│   • Refreshes sales order list             │
└─────────────────────────────────────────────┘
```

---

## Component Interaction Flow

```
┌──────────────────────────┐
│    SalesOrders.tsx       │  Main Container
│    (Parent Component)    │
└────────────┬─────────────┘
             │
             ├─────────────────────────┐
             │                         │
             ↓                         ↓
┌────────────────────────┐   ┌─────────────────────────┐
│  SalesOrderList.tsx    │   │ SalesOrderDetails.tsx   │
│                        │   │                         │
│  • Fetches all orders  │   │  • Shows selected order │
│  • Displays in table   │   │  • Tabs for sections    │
│  • Handles selection   │   │  • Edit/Delete actions  │
└────────────────────────┘   └─────────────────────────┘
             ↑                         ↑
             │                         │
             └─────────┬───────────────┘
                       │
                       │ Uses
                       ↓
            ┌──────────────────────┐
            │  salesOrderApi.ts    │
            │                      │
            │  • getAllSalesOrders │
            │  • getSalesOrderById │
            │  • getContacts       │
            │  • getComments       │
            └──────────────────────┘

┌────────────────────────────────────┐
│  CreateSalesOrderModal.tsx         │
│                                    │
│  • Opens on "Create" button        │
│  • 3-step wizard                   │
│  • Fetches dropdown data on mount  │
│  • Submits to API                  │
└────────────────────────────────────┘
             ↑
             │ Uses
             ↓
┌────────────────────────────────────┐
│  salesOrderApi.ts                  │
│                                    │
│  • getMaterialTypes()              │
│  • getPaymentTermTypes()           │
│  • createSalesOrder()              │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│  SalesOrderApproval.tsx            │
│                                    │
│  • Shows pending orders            │
│  • Approve/Reject buttons          │
│  • Reason input for rejection      │
└────────────────────────────────────┘
             ↑
             │ Uses
             ↓
┌────────────────────────────────────┐
│  salesOrderApi.ts                  │
│                                    │
│  • getAllSalesOrders()             │
│  • approveSalesOrder()             │
│  • rejectSalesOrder()              │
└────────────────────────────────────┘
```

---

## New Fields Integration Map

### Sales Order Details Section

```
┌────────────────────────────────────────────────┐
│  SALES ORDER DETAILS                           │
├────────────────────────────────────────────────┤
│                                                │
│  [Sales Order Type ▼]  ← NEW (Required)       │
│    Options: Sales Order, Work Order            │
│                                                │
│  [Lead Number        ]  ← NEW (Optional)       │
│                                                │
│  [Quotation ▼]         ← Existing              │
│  [Business Name     ]  ← Auto-filled           │
│  [Customer Branch ▼ ]  ← Existing              │
│  [Contact Person ▼  ]  ← Existing              │
│  [BOM Number        ]  ← Auto-filled           │
│  [Total Cost        ]  ← Auto-filled           │
│  [Currency ▼        ]  ← Existing              │
│  [SO Date           ]  ← Existing              │
│  [Comments          ]  ← Existing              │
│                                                │
└────────────────────────────────────────────────┘
```

### Project Details Section

```
┌────────────────────────────────────────────────┐
│  PROJECT DETAILS                               │
├────────────────────────────────────────────────┤
│                                                │
│  [Work Order Number       ]  ← Existing        │
│  [Work Order Tenure (months)]  ← NEW          │
│  [Project Name            ]  ← NEW            │
│                                                │
│  ❌ Manpower/Service Quotation  ← REMOVED     │
│                                                │
│  [Work Order Amount       ]  ← Existing        │
│  [Work Order Date         ]  ← Existing        │
│  [Project Category ▼      ]  ← Existing        │
│  [Project Template ▼      ]  ← Existing        │
│  [Project Address         ]  ← Existing        │
│  [Project Start Date      ]  ← Existing        │
│  [Project End Date        ]  ← Existing        │
│                                                │
└────────────────────────────────────────────────┘
```

### Bank Guarantee Section

```
┌────────────────────────────────────────────────┐
│  BANK GUARANTEE INFORMATION                    │
├────────────────────────────────────────────────┤
│                                                │
│  [Issue Date           ]  ← NEW                │
│                                                │
│  Beneficiary Details:                          │
│    [Name    ] [Address ] [Contact] [Email]    │
│                                                │
│  Applicant Details:                            │
│    [Name    ] [Address ] [Contact] [Email]    │
│                                                │
│  Bank Details:                                 │
│    [Name    ] [Address ] [Contact] [Email]    │
│                                                │
│  [Guarantee Number     ]  ← Existing           │
│  [Currency ▼           ]  ← Existing           │
│  [Guarantee Amount     ]  ← Existing           │
│  [Effective Date       ]  ← Existing           │
│  [Expiry Date          ]  ← Existing           │
│  [Purpose ▼            ]  ← Existing           │
│                                                │
│  [Guarantee Type ▼     ]  ← MODIFIED           │
│    Options:                                    │
│    • ADVANCE PAYMENT GUARANTEE                 │
│    • BID BOND                                  │
│    • FINANCIAL GUARANTEE                       │
│    • PERFORMANCE GUARANTEE                     │
│                                                │
│  ❌ Exemption Type  ← REMOVED                  │
│                                                │
└────────────────────────────────────────────────┘
```

### Material Type Section

```
┌────────────────────────────────────────────────┐
│  MATERIAL TYPE                                 │
├────────────────────────────────────────────────┤
│                                                │
│  [Material Type ▼      ]  ← NEW (from API)     │
│                                                │
└────────────────────────────────────────────────┘
```

### Payment Terms Section

```
┌────────────────────────────────────────────────┐
│  PAYMENT TERMS                                 │
├────────────────────────────────────────────────┤
│                                                │
│  [+ Add Payment Term]                          │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │ Description: [              ]            │ │
│  │ Term Type: [▼ From API] ← MODIFIED       │ │
│  │ Percentage: [   ] %                      │ │
│  │ Amount: ₹ [auto-calculated]              │ │
│  │ [🗑 Remove]                               │ │
│  └──────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
```

---

## TypeScript Type Flow

```typescript
// Form State (Frontend)
interface FormData {
  salesOrderType: 'Sales Order' | 'Work Order';  // NEW
  leadNumber: string;                             // NEW
  workOrderTenureMonths: number;                  // NEW
  projectName: string;                            // NEW
  issueDate: string;                              // NEW
  guaranteeType: string;                          // MODIFIED
  materialTypeId: string;                         // NEW
  // ... existing fields
}
     ↓
// Transform to API structure
     ↓
interface SalesOrderPayload {
  salesOrder: {
    sales_order_type: string;      // From salesOrderType
    lead_number: string;            // From leadNumber
    // ... other fields
  };
  projectDetails: {
    work_order_tenure_months: number;  // From workOrderTenureMonths
    project_name: string;               // From projectName
    // ... other fields
  };
  bankGuaranteeInfo: {
    issue_date: string;            // From issueDate
    guarantee_type: string;        // From guaranteeType
    // ... other fields
  };
  // ... other sections
}
     ↓
// Send to API
     ↓
// Backend processes and stores
     ↓
// Returns SalesOrder entity
     ↓
interface SalesOrder {
  sales_order_id: string;
  sales_order_number: string;
  sales_order_type: string;
  lead_number: string;
  // ... all fields
}
```

---

## State Management Flow

```
┌────────────────────────────────────────────┐
│  Component State                           │
├────────────────────────────────────────────┤
│                                            │
│  • formData (CreateSalesOrderModal)        │
│  • salesOrders[] (SalesOrderList)          │
│  • selectedSalesOrder (SalesOrderDetails)  │
│  • materialTypes[] (from API)              │
│  • paymentTermTypes[] (from API)           │
│  • loading (boolean)                       │
│  • error (string | null)                   │
│                                            │
└────────────────────────────────────────────┘
     ↕
[User Interaction]
     ↕
┌────────────────────────────────────────────┐
│  Event Handlers                            │
├────────────────────────────────────────────┤
│                                            │
│  • handleInputChange()                     │
│  • handleSubmit()                          │
│  • handleApprove()                         │
│  • handleReject()                          │
│  • handleDelete()                          │
│                                            │
└────────────────────────────────────────────┘
     ↕
[API Calls]
     ↕
┌────────────────────────────────────────────┐
│  API Functions (salesOrderApi.ts)          │
├────────────────────────────────────────────┤
│                                            │
│  • Returns: Promise<T>                     │
│  • Handles: axios requests                 │
│  • Error handling: try/catch               │
│                                            │
└────────────────────────────────────────────┘
     ↕
[Backend API]
     ↕
┌────────────────────────────────────────────┐
│  State Update                              │
├────────────────────────────────────────────┤
│                                            │
│  • setState() with response data           │
│  • Update loading state                    │
│  • Handle errors                           │
│  • Show notifications                      │
│                                            │
└────────────────────────────────────────────┘
```

---

## Summary

✅ **Completed:**
- API utility layer with all functions
- TypeScript interfaces for type safety
- Environment configuration
- Documentation (3 comprehensive guides)
- Build verification

📋 **Pending:**
- Update CreateSalesOrderModal with new fields
- Replace mock data in SalesOrderList
- Implement API calls in SalesOrderDetails
- Implement approval workflow in SalesOrderApproval

🎯 **Impact:**
- Only Sales Order module affected
- No changes to other modules
- Clean, maintainable architecture
- Follows existing patterns
